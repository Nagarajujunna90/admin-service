server:
  port: 8083
spring:
  application:
    name: admin-service
  datasource:
    password: root
    url: jdbc:mysql://localhost:3306/e-user?useSSL=true
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
  instance:
    hostname: localhost